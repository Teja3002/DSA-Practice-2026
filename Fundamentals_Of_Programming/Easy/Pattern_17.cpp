cout << ch;
            }

            // decreasing letters
            for (char ch = 'A' + i - 2; ch >= 'A'; ch--) {
                cout << ch;
            }

            cout << endl;
        }
    }
};